usecase:
    - async nature
    - retry capability
    - pace matching
        - send message is fast but consumer takes at its own pace.


point to point - 
    - it can be consumed by any consumer but only once
pub sub - 
    - based on exchnage logic sendsd message to all queue
    - same message can be consumed by many consumers


Kafka:
    -  